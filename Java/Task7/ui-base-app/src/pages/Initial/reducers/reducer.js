const initialState = {
  availableItems: [
    'how+to+lear+js',
    'somePath',
    'Картинка',
    'anotherPath',
  ],
};

export default (state = initialState, {type, payload}) => {
  switch (type) {

    default: return state;
  }
}
